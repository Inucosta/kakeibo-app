# Django シェルでの基本的な操作メモ
python manage.py shell


#################
## 口座更新処理 ##
#################

# idの口座を取得
account = Account.objects.get(id=4)

# 残高を 5000 に変更
account.balance = 8372

# 保存
account.save()

########################
## カテゴリ一覧の表示  ##
########################
print(list(Category.objects.values_list('name', flat=True)))

from yourapp.models import Category

# 例えばIDが7と8のカテゴリを投資に変更
Category.objects.filter(id__in=[7, 8]).update(type='investment')
